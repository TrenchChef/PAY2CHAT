[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install --production=false"]

[phases.build]
cmds = ["echo 'Skipping Next.js build - running signaling server only'"]

[start]
cmd = "node server.js"

