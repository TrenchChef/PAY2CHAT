/**
 * Stage 2: Basic Call UI Page
 * 
 * Wrapper page that uses the CallUI component
 */

'use client';

import { CallUI } from '@/components/CallUI';

export default function CallPageClient() {
  return <CallUI />;
}
