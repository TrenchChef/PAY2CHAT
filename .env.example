# Copy this file to .env and replace the placeholder with your real key
# Do NOT commit your .env file — it is ignored by .gitignore

# Metered API key for any server-side metered services (example)
METERED_API_KEY=your_metered_api_key_here

# Optional: port for the signaling server
PORT=8888
